sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t aviationfan/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true aviationfan/docker-react npm run test
